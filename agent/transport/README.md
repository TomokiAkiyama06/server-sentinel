# Agent Transport

Owns agent-initiated authenticated private-LAN connection, bounded media/control delivery, reconnect/backpressure, and heartbeat-loss signals used by local incident protection.

Keep the exact media transport subject to the planned PoC/ADR. Validate source/session and size/rate bounds; never accept arbitrary output paths, expose human/dashboard routes, require inbound SSH administration, or connect browser viewers directly to the agent.
