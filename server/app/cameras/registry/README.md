# Camera and Capture Node Registry

Owns logical source/node identities, source type and role metadata, profile bindings, activation limits, and separate source/node health records for `local_uvc` and `remote_agent`.

Do not use fixed front/rear columns, replace a source silently when the limit is reached, or use `/dev/videoN` as durable physical identity.
