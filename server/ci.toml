version = 1

[checks]
lint = ["python", "-m", "tests.lint"]
test = ["python", "-m", "unittest", "discover", "-s", "tests", "-p", "test_*.py", "-v"]

[smoke]
dockerfile = "Dockerfile.ci"
normal = ["python", "-m", "tests.smoke", "normal"]
error = ["python", "-m", "tests.smoke", "error"]
