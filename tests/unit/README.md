# Unit Tests

Owns isolated domain checks for authorization, Camera Source identity/limits, event/presence rules, buffer/incident lifecycle, storage admission, and integrity-state decisions using controlled clocks and synthetic inputs.

Do not require a live camera, GPU, Tailnet, Slack endpoint, or real deployment credentials/media. Avoid treating mocked hardware identity or successful unit checks as physical acceptance.
