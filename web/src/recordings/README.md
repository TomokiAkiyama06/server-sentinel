# Recordings

Owns `recordings:view`-authorized recording lists, metadata, and browser playback; owner-only actions include deletion and applicable recording/storage controls.

Playback manifests/segments remain server-authorized. Do not provide non-owner download/export controls, make copied playback URLs public, or claim playback prevents screen recording/client capture.
